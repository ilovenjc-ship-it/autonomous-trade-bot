# RESEARCH LOG — reviewed & parked

A lightweight index of articles, videos, products, threads, and SDKs that
have been reviewed but **did not warrant a dedicated entry in STATE.md**.
Items here are "reviewed → parked → revisitable." If a parked item later
becomes actionable, it graduates to a STATE.md research entry.

The full review for each item lives in the chat transcript on the date
shown — this file is the index, not the report.

| Date | Item | Source | Verdict | Revisit when |
|---|---|---|---|---|
| 2026-05-26 | **Zyfai — Personal Yield Agent (TypeScript SDK)** | [zyf.ai](https://www.zyf.ai/) · [`@zyfai/sdk`](https://www.npmjs.com/package/@zyfai/sdk) v0.2.36 | Direct value **zero**, pattern value **medium**. EVM stablecoin yield aggregator (Arbitrum / Base / Plasma · USDC / USDT) — different chain, different asset class, paid API, zero Bittensor / TAO / alpha mention. Patterns worth borrowing later: (a) two-level user strategy abstraction (`conservative` ↔ `aggressive`); (b) session-key delegated execution (ERC-7579 / Safe7579 / SmartSession); (c) first-class agent-control SDK methods (`pauseAgent()`, `enableSplitting(n)`, `withdrawFunds()`). | Productization-tier conversation — when the App is self-sustainable and a managed-Ari SDK / public API surface needs to be designed for third-party operators. The shape of their SDK methods is a fast template; their session-key delegation is the closest EVM analog to whatever Substrate-side primitive we'd need for third-party τ delegation. |
| 2026-05-26 | **Mamo — DeFi savings agent (Moonwell consumer app)** | [mamo.bot](https://mamo.bot/) · [docs.mamo.bot](https://docs.mamo.bot/) · [`moonwell-fi/mamo-contracts`](https://github.com/moonwell-fi/mamo-contracts) · [Halborn audits](https://www.halborn.com/audits/moonwell/) | Direct value **zero**, pattern value **high**. Structurally Moonwell's consumer app (Luke Youngblood as core contributor; contracts repo lives under `moonwell-fi`; audits filed under halborn.com/audits/**moonwell**/). 28-contract on-chain footprint, Base / Optimism, USDC/cbBTC vaults. Six patterns worth borrowing: (1) **per-user Strategy contracts** — each user gets their own deployed contract, the on-chain analog of per-user hotkey delegation on Bittensor; (2) **PauseGuardian vs Multisig role separation** — fast pause role (single-purpose, hot, can only halt) decoupled from slow upgrade-authority role (cold, multisig, can change code) — prevents the "the key that can pause is also the key that can rug" failure mode; (3) **asymmetric capability split** — backend can rebalance into whitelisted strategies but cannot withdraw to arbitrary addresses, so a hot-key compromise can't exfiltrate; (4) **$5 auto-compound threshold** — gas-aware action gating, action only fires when expected economic benefit exceeds expected execution cost (direct mirror of the slippage / fee floor logic in `execution_guard.py`); (5) **on-chain Chainlink Slippage Checker** — oracle-enforced execution bounds at the contract layer, not just the agent layer (the on-chain version of our pre-flight gate); (6) **risk-disclosure doc pattern** — docs explicitly enumerate what can fail (oracle deviation, vault insolvency, stablecoin depeg, smart-contract bug) before describing what the product does. Tokenomics also Const-doctrine-shaped: zero VC, no day-1 unlocks, ecosystem-aligned airdrop, locked LP, 24-month linear vest, 6-month team cliff. **One anti-pattern flagged**: their "highest available rate" single-objective optimizer is the wrong shape for prediction-contested outputs (alpha emission) — single-optimizer is correct for contested-by-pricing inputs (stablecoin yield) but collapses to mode-chasing when the target is a forecast. Reinforces why the 7/12 supermajority gate exists on our side. | Productization tier — when we need to design (a) tokenomics for a public token / fair launch, (b) on-chain footprint of any user-funds-touching contracts, and (c) risk-disclosure docs for a public-facing product. Mamo's contract topology + role-separation diagram is a fast reference template; their docs-as-disclosure pattern is the right shape for any public risk surface we publish. Anti-pattern (single-optimizer) goes into `ANTI_PATTERNS.md` if/when that file is created — flagged here for now. |
| 2026-05-26 | **Bankr — AI crypto banker (Base / Farcaster / Claude Code plugin)** | [bankr.bot](https://bankr.bot/) · [docs.bankr.bot](https://docs.bankr.bot/) · [`BankrBot/skills`](https://github.com/BankrBot/) · [Bitrue: Claude integration](https://www.bitrue.com/blog/how-to-use-bankrbot-with-claude-ai-trading) · [iq.wiki/wiki/bankr](https://iq.wiki/wiki/bankr) · BNKR `0x22aF33FE49fD1Fa80c7149773dDe5890D3c76F3b` (Base) | Direct value **zero** (different chain, different asset class — Base / EVM memecoin token-launches, no Bittensor / TAO / α / subnet-pool curvature). Pattern value **high — one standout pattern that Mark elevated to project-goal status, plus one in-the-wild failure that validates our architecture.** **THE STANDOUT PATTERN — self-funding LLM economics (Mark's directive 2026-05-26: "That's the goal eventually for our Project — to be fully autonomous"):** trading volume → 1.2% swap fee → 57% to launcher / ~2% to Bankr LLM Gateway → Claude+Gemini+GPT API calls "at cost." The agent literally pays its own inference bills from the trade flow it produces. Not subscriptions, not VC, not user pre-paid credits, not advertising. **Volume → fees → inference budget**, automatic, real-time. For Ari at productization tier this is the structural answer to "how does Ari pay for its own intelligence at scale" — TAO/α trades have implicit cost (slippage + emission share); a productized Ari layer captures a tiny fraction of the value it creates and routes it to inference. **THE STANDOUT ANTI-PATTERN — the Grok incident (March 2025):** *"Grok misinterpreted user inputs, resulting in the unintended generation of cryptographic tokens. These tokens were automatically distributed and became tradable on decentralized exchanges (DEXs) before the issue was identified."* Single LLM with autonomous mint authority on misinterpreted natural-language input → real tokens hit real DEX pools → emergency DEX-operator coordination required → bolted-on "multi-layered verification checks" after the fact. **This is the in-the-wild empirical case for our 7/12 OpenClaw supermajority gate.** Every time the gate's complexity cost is questioned, Grok is the answer — single-optimizer LLMs given execution authority WILL eventually misinterpret an input, and when they do on-chain, the cleanup is "call DEX operators and beg." Not a recoverable position. We built the gate first; Bankr learned the lesson the hard way. **Other borrowable patterns:** (1) API-key custody with IP whitelisting + granular permissions ("No private keys exposed") — same shape as Mamo's asymmetric capability split, one layer up; (2) Skills marketplace as agent extension (install from any public GitHub repo, each carries "experimental security scans") — Claude-Code-native version of community-contributed Ari strategies; (3) CLI-first developer surface (`bankr login` / `bankr launch` / `bankr fees --token 0x...`) — fits the gh/git/Claude-Code shape; (4) **Distribution thesis: plug into where the developer already lives** (Claude Code, Cursor, OpenClaw) — Mamo went mobile-consumer, Zyfai went embeddable-SDK, Bankr went IDE-plugin; three legitimate productization channels documented. **Other anti-patterns:** opaque tokenomics (100B BNKR, "deployed by the AI agent itself" via Farcaster, no published vesting / allocation — direct contrast with Mamo's Const-doctrine full disclosure); fee split with a missing 41% slice (57% launcher + ~2% LLM gateway = 59%; remaining 41% unaccounted in public marketing copy); marketing claim "Battle-tested — proven reliability across tens of thousands of transactions" written *after* the Grok incident required emergency DEX cleanup (don't write "battle-tested" if there's a recent incident that needed off-platform cleanup). | Productization tier — when designing **(a) self-funding inference economics for Ari** (row's primary trigger, elevated to project-goal by Mark 2026-05-26: trade-flow → fee capture → LLM budget routing, the structural autonomy answer); **(b) IDE-plugin distribution channel** as one of three documented productization surfaces (mobile-consumer / embeddable-SDK / IDE-plugin). Grok-incident anti-pattern is a candidate for promotion to `ANTI_PATTERNS.md` when that file is created — strongest in-the-wild validation of the 7/12 supermajority gate found to date. |
| 2026-05-26 | **NawafSheikh — "I built an autonomous crypto trading desk where AI agents debate every trade"** | [youtube.com/watch?v=xP2AIss6RqI](https://www.youtube.com/watch?v=xP2AIss6RqI) · [nawaf.eu](https://nawaf.eu/) · channel `@nawaf_sheikh` (28 subs · video uploaded 2026-05-25 · 6 views at review time) | Direct value **zero** (different chain — SOL/USDT on unspecified exchange · paper-only · no repo · no benchmark numbers · explicit *"Sample portfolio numbers are used for the demo"* · *"a faithful recreation of the actual dashboard"* — product theater, not a product). Pattern value **medium-low directly, high as architecture-validation in reverse.** **Their pipeline:** Price Watcher → Lead Analyst (fans out to 3 specialists: Chart / News / Sentiment) → Judge Agent (debates bull-vs-bear, emits buy/sell/hold + confidence + entry + SL + target) → Risk Manager (gates: size, drawdown, mandatory stop, confidence floor) → Executor → Reflection Agent (midnight review + adjusts). Queue-based multi-agent orchestration across **Claude model tiers** (fast model for routine, stronger model for debate + nightly review), live Next.js dashboard. **Architectural mirror — opposite of OpenClaw on every axis:** topology hierarchical (1 lead → 3 specialists → 1 judge → 1 RM) vs ours flat (12 independent strategies vote into supermajority); decision substrate LLM-mediated debate vs ours heuristic strategies (RSI/BB/MACD/BTC-divergence); aggregation single-judge synthesis vs 7-of-12 supermajority; SPOF the judge agent vs none (any one strategy wrong is bounded); bias correlated through lead-analyst framing vs uncorrelated by flat-vote construction; falsifiable nope (no baseline, no real numbers, no repo) vs yes (1955 cycles · 4379 paper trades · 7-day fleet WR vs 55% gate vs SIDEWAYS-regime 50% coin-flip baseline · public commit history). **Borrowable patterns (3, all small):** (1) two-tier LLM economics (fast for routine / stronger for debate + nightly review) — applicable IF/WHEN we add an LLM analysis/narration layer to the dashboard, not in the trade loop; (2) explicit per-strategy verdict format with SL + target — currently our `_compute_signal` returns BUY/SELL/None + `signal_reason` and SL/target lives uniformly in the executor; per-strategy SL would let mean-rev set tighter than momentum; (3) reflection agent for nightly review **only with bounded mutation space** (adjust strategy weights within band, never rewrite strategy logic — Mark currently does this manually with full context, the bounded version is the right shape if we ever automate). **Anti-pattern reinforcement (the bigger signal):** **second in-the-wild example of single-LLM-as-trade-judge** (Grok was the first). Pattern is now *generic*, not a one-off. The structural critique stands: a confidence-gated judge agent cannot catch a coherent-but-wrong thesis — the risk manager only enforces size/SL/drawdown, never second-guesses direction. Our 7-of-12 supermajority gate is the structural fix because it requires *direction agreement across independent classifiers*. Mark's read 2026-05-26 verbatim: *"flat-vote heuristic supermajority is now backed by two in-the-wild counterexamples of why hierarchical-LLM-judge fails."* | (a) When designing the **LLM analysis/narration layer** if/when we add one to the dashboard — two-tier model economics + bounded-reflection-agent shape; (b) when designing the **per-strategy verdict format** if we hoist SL/target out of the executor into `_compute_signal` (small refactor, real diversity gain). **Anti-pattern entry candidate** for `ANTI_PATTERNS.md` when that file is created — together with Mamo's single-optimizer flag (Mamo) and the Grok incident (Bankr row), this row populates the **agent-trading anti-pattern triad: single optimizer / unguarded LLM minter / single LLM judge**. The triad is the cumulative empirical case for OpenClaw's architecture. |

---

## Filing conventions

- **Date** is the date of review.
- **Item** is the name with a one-line subtitle if needed.
- **Source** is the canonical URL(s) — primary site, repo, package, paper.
- **Verdict** captures direct relevance + pattern relevance separately, with the why.
- **Revisit when** names a concrete trigger condition, not a date.

If a row's "Revisit when" condition fires, that's the cue to either:
- Open a new chat and re-review it against the then-current state of the project, or
- Promote it to a STATE.md research entry with full context.

Items that turn out to be actively misleading or anti-pattern get filed
into `ANTI_PATTERNS.md` instead, not here.